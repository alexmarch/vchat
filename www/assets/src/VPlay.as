package  {		import flash.display.MovieClip;	import flash.net.NetConnection;	import flash.net.NetStream;	import flash.events.NetStatusEvent;			public class VPlay extends MovieClip {		private var nc:NetConnection;		private var ns:NetStream;				public function VPlay() {			// constructor code			initConnection("rtmp://127.0.0.1/videochat");		}		public function initConnection(host:String):void{			nc = new NetConnection();			nc.addEventListener(NetStatusEvent.NET_STATUS,netStatusHandler);			nc.client = this;			nc.connect(host);		}		public function playStream(name:String):void{			ns = new NetStream(nc);			ns.play(name);			view.attachNetStream(ns);		}		private function netStatusHandler(event:NetStatusEvent):void{			trace(event.info.code);			switch(event.info.code){				case "NetConnection.Connect.Success":					playStream("room211_7JwW7gbnv0wTU4hfkqWB3SenvY8TyBz7jCfnTIiOWqQ=");					break;			}		}	}	}